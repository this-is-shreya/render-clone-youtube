<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Free Hosting</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <h4><u>Host a Static Site</u></h4>
    <br />
    <form action="/host/static/static/<%=state%>" method="post">
      <%if (state=="update"){%> Title of your project:<input
        type="text"
        name="title"
        value="<%= title%>"
        class="form-control"
        readonly
      /><br />Repository:
      <select name="repo" id="repo" class="form-select" readonly>
        <option value="<%= repo%>" selected><%= repo%></option></select
      ><br />
      <%}else{%> Title of your project:<input type="text" name="title" /><br />
      Repository:
      <select name="repo" id="repo" class="form-select">
        <%repo.forEach(rep=>{%>
        <option value="<%= rep.name%>"><%= rep.name%></option>
        <%})%></select
      ><br />
      <%}%> Branch:<input
        type="text"
        name="branch"
        class="form-control"
      /><br />

      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </body>
</html>
